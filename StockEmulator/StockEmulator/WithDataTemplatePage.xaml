<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StockEmulator.WithDataTemplatePage">
  <StackLayout Padding="0,20,0,0">
    <Label
      Text="ListView with a DataTemplate" FontAttributes="Bold" />
    <ListView x:Name="listView">
      <ListView.ItemTemplate>
        <Datatemplate>
          <ViewCell>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5*"/>
                <ColumnDefinition Width="0.2*"/>
                <ColumnDefinition Width="0.3*"/>
              </Grid.ColumnDefinitions>
              <Label Text="{Binding Name}" FontAttributes="Bold" />
              <Label Text="{Binding Age}"  Grid.Column="1"/>
              <Label Text="{Binding Location}" Grid.Column="2" HorizontalTextAlignment="End" />
            </Grid>
          </ViewCell>
        </Datatemplate>
      </ListView.ItemTemplate>
    </ListView>
      
  </StackLayout>
</ContentPage>